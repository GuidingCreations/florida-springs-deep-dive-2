import React from 'react'
import { getUserProfilePicture } from '@/lib/actions/user.action'

const ImageResourceGenerator = (ImageURL : string) => {
  return (ImageURL)
}

export default ImageResourceGenerator